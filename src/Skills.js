function Skills() {
    return (
        <section className="
            h-screen
            flex
            justify-center
            items-center
            text-city-lights">
        </section>
    );
}

export default Skills;